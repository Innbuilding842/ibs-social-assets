# ibs-social-assets

Public image host for IBS social posts. Meta fetches carousel images from raw.githubusercontent.com URLs pinned to a commit SHA, so every post gets immutable URLs and no cached-image collisions. Written by ~/.openclaw/workspace/ibs-assistant/scripts/meta_publish.py; nothing here is private (every file is a published or approved-for-publish marketing image).
